templates:
  - ./templates/main.yml
  - ./templates/plugin.yml

lifecycles:
  start:
    services:
      - name: one
        action: start
      - name: two
        action: start
  stop:
    services:
      - name: one
        action: stop
      - name: two
        action: stop
