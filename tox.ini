[tox]
envlist=py26,py27,py35

[testenv]
deps=   nose
        coverage
        mock

commands=   coverage erase
            coverage run {envbindir}/nosetests
            coverage html --include=*docknv* --omit=*test*,*.tox*